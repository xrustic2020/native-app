import RN from 'react-native';

export default RN.StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  heading: {
    marginBottom: 50,
    fontWeight: 'bold',
    fontSize: 24,
  },
  message: {
    marginBottom: 10,
  },
});
